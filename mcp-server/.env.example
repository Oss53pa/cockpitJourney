# Personal Access Token issued from CockpitJourney → Settings → Integrations.
# Required. Format: cj_<base64url>.
CJ_PAT=cj_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Supabase project URL. Defaults to Atlas Studio production if unset.
SUPABASE_URL=https://vgtmljfayiysuvrcmunt.supabase.co

# Public anon key for the Supabase project. Required (we use it as the
# `apikey` header for Edge Function and PostgREST calls). Get it from
# Supabase Dashboard → Project Settings → API.
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
